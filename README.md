このプロジェクトでやりたいことはmacosのcliアプリをios上で動かすこと。

必要なのはmacのsyscallなどの仮想化レイヤをios上に構築すること、と
dyldとlibsystemと疑似ファイルシステム、mach-oローダ。

すべてを非脱獄のiosアプリ内に内包させる。

決してmacのアプリをiosにクロスコンパイルすることではない。

guiアプリには対応させない。
cliアプリのみ完全対応させる。
